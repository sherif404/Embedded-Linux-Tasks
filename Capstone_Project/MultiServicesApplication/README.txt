-All used libraries in the default path in my Host machine and in the Raspberry pi4 " /lib " , "usr/local/lib"
-the logfile has a function that rotate the generation of the received messages queue to new logfile when the currant files exceeds 1000 line, the function is provided in LogerDeamon.

